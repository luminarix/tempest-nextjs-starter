{
  "name": "luminarix/tempest-nextjs-starter",
  "description": "Full-stack starter with Tempest PHP backend and Next.js frontend",
  "type": "project",
  "license": "MIT",
  "keywords": [
    "tempest",
    "nextjs",
    "fullstack",
    "starter",
    "docker"
  ],
  "require": {
    "php": "^8.4"
  },
  "scripts": {
    "post-create-project-cmd": [
      "cp backend/.env.example backend/.env",
      "cp frontend/.env.example frontend/.env",
      "rm -rf composer.json composer.lock vendor",
      "echo && echo '\\033[32m✓ Project created!\\033[0m' && echo && echo '  Run:' && echo \"    \\033[36mcd $(basename $PWD)\\033[0m\" && echo '    \\033[36mdocker compose up -d\\033[0m' && echo"
    ]
  },
  "config": {
    "sort-packages": true
  }
}
